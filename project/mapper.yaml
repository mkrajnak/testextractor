component:
  arches:
  - all
  name: <app>

dependencies:
  beaker-tasks:
  - /desktop/rhel8/install
  packages:
<packages>

beaker-hardware:
- x86_64:
    group: desktop
    variant: Workstation

setup:

cleanup:

testmapper:
