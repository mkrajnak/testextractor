component:
  arches:
  - all
  name: <app>

dependencies:
  beaker-tasks:
  - /desktop/rhel8/install
  packages:
  - <app>

beaker-hardware:
- x86_64:
    group: desktop
    variant: Workstation

setup:

cleanup:

testmapper:
  - start_via_command
# Generated part of mapper