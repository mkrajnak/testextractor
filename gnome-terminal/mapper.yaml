component:
  arches:
  - all
  name: gnome-terminal

dependencies:
  beaker-tasks:
  - /desktop/rhel8/install
  packages:
  - gnome-terminal

beaker-hardware:
- x86_64:
    group: desktop
    variant: Workstation

setup:

cleanup:

testmapper:
  - start_via_command
# Generated part of mapper