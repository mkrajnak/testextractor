component:
  arches:
  - all
  name: gnome-terminal

dependencies:
  beaker-tasks:
  - /desktop/rhel8/install
  packages:
<packages>

beaker-hardware:
- x86_64:
    group: desktop
    variant: Workstation

setup:

cleanup:

testmapper:
# Generated part of mapper
  - 0_NewWindow
